# VEH website

Static product website for [veh.app](https://veh.app).

The site is designed for GitHub Pages and requires no server, build step, database, or runtime configuration. GitHub Pages should publish the root of the `main` branch. The `CNAME` file keeps the custom domain set to `veh.app`.
